import React, {FC} from 'react';
import cls from './categorySection.module.scss'

interface T {
    children?: any
}

const CategorySection: FC<T> = ({children}) => {
    return (
        <section className="_vrm">

        </section>
    );
};

export default CategorySection;